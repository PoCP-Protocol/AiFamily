import { FamilyScreenList } from "@/components/family/family-screen-list";

export default function GrowthScreen() {
  return (
    <FamilyScreenList
      tab="growth"
      eyebrow="成长与计划"
      title="一起看见我们家的节奏"
      description="从真实家庭场景出发，先看一份待确认的支持方向，再由家庭共同选择是否进入计划；每一步都保留来源、边界与暂停空间。"
    />
  );
}
